{% extends "base.html.twig" %}

{% block title %}Alterar telefone do usuário "{{ phone.getUsers.name }}"
{% endblock %}

{% block body %}
	<div class="container mx-auto max-w-7xl px-4">

		<h2 class="text-4xl font-semibold mb-2">Alterar telefone do usuário "{{ phone.getUsers.name }}"</h2>
		{{ form(form) }}
		</div>
	{% endblock %}
