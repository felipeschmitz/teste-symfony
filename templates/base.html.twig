<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>
		{# Run `composer require symfony/webpack-encore-bundle`
																																																																								           and uncomment the following Encore helpers to start using Symfony UX #}
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<script src="https://cdn.tailwindcss.com"></script>
		{% block stylesheets %}
			{#{{ encore_entry_link_tags('app') }}#}
		{% endblock %}

		{% block javascripts %}
			{#{{ encore_entry_script_tags('app') }}#}
		{% endblock %}
	</head>
	<body>
		<div class="min-h-screen bg-slate-100">
			<nav class="bg-white shadow mb-4">
				<div class="container mx-auto max-w-7xl px-4">
					<div class="flex justify-between">
						<div class="flex space-x-4">
							<a href="{{ path('list_users') }}">
								<h1 class="text-2xl py-3 px-2 font-semibold uppercase">Teste</h1>
							</a>
							<div class="flex items-center space-x-1">
								<a class="py-5 px-3 rounded text-gray-700 transition duration-300 hover:text-gray-900" href="{{ path('list_users') }}">Usuários</a>
								<a class="py-5 px-3 rounded text-gray-700 transition duration-300 hover:text-gray-900" href="{{ path('list_phones') }}">Telefones</a>
							</div>
						</div>
					</div>
				</div>
			</nav>
			{% block body %}{% endblock %}
		</div>
	</body>
</html>
